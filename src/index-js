angular.module('pmkr.componentsDemo', [
  'ui.router',
  'ui.bootstrap',
  'pmkr.components'
])

.config([
  '$urlRouterProvider',
  '$locationProvider',
  function($urlRouterProvider, $locationProvider) {
    //$locationProvider.html5Mode(true);
    $urlRouterProvider.otherwise('/');
  }
])

.config([
  '$stateProvider',
  function($stateProvider) {

    $stateProvider
    .state('home', {
      url: '/'
    })
    <%
    for (var componentTypeName in data) {
      var componentType = data[componentTypeName];
      for (var componentName in componentType) {
        var component = componentType[componentName];
        if (component.samples) {
    %>
    .state('<%= componentName %>', {
      url: '/<%= _(componentName).dasherize() %>',
      templateUrl: '/pages/<%= componentName %>.html'
    })
    <%
        }
      }
    }
    %>
    ;

  }
])